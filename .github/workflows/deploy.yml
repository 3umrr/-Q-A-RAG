name: Build and Push Backend to Azure ACR

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Check out the code from the repository
    - name: Checkout Code
      uses: actions/checkout@v4

    # Step 2: Set up Docker Buildx (for faster builds and caching)
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    # Step 3: Log in to Azure Container Registry (ACR) using GitHub Secrets
    - name: Log in to Azure Container Registry
      uses: docker/login-action@v3
      with:
        registry: qaragacr.azurecr.io
        username: ${{ secrets.ACR_USERNAME }}
        password: ${{ secrets.ACR_PASSWORD }}

    # Step 4: Build the Docker image and push it to ACR
    - name: Build and Push Docker Image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: backend/Dockerfile
        push: true
        tags: |
          qaragacr.azurecr.io/qarag-fastapi:latest
          qaragacr.azurecr.io/qarag-fastapi:${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
